name: Build with MCU Expresso

on:
  push:
    branches: [ develop ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: 'true'

      - name: Replace version token
        run: |
          CUT_VERSION=$(printf '%.15s\n' "${{ github.sha }}" )
          find src/mod/l7_climb_app.h -type f -exec sed -i "s/@TKN_SWRELEASE@/$CUT_VERSION/g" {} \;
          
      - name: Build the mcu workspace
        uses: RobertK66/actions-build-mcuxpresso@main
        id: publish-document
        with:
          project:  ClimbObc
          
      - name: Upload Binary
        uses: actions/upload-artifact@v2
        id: upload-axf
        with:
          name: 'obc-axf'
          path: ${{ steps.publish-document.outputs.axf }}
          
